<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javalec.ex.dao.ADBMtmDao">

	<select id="getAllBoards" resultType="HashMap">
		select iu_title, iu_date from mtm_inqury_user_tb
	
	</select>

		<!--  

	<select id="getAllBoards" resultType="HashMap">
		select rownum rownum, mem.m_id m_id, mtm.iu_title iu_title,
		mtm.iu_date iu_date, mtm.iu_status iu_status
		 from member_tb mem, mtm_inqury_user_tb mtm where mem.m_num = mtm.m_num
		order by mtm.iu_num desc		
	</select>
	<select id="getAllBoards" resultType="com.javalec.ex.dto.MtmUserDto">
		
	</select>
-->

</mapper>